# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Env
.env

# Node (we don't use it but guard anyway)
node_modules/

# Git worktrees (per project convention; never commit worktree contents)
.worktrees/

# Sensitive work data — NEVER commit
*.pdf
*.csv
CoWork/

# Allow documented example fixtures if we ever add them
!examples/**/*.pdf
!examples/**/*.csv
