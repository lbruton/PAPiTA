# CLAUDE.md

This file provides guidance to Claude Code when working with code in this repository.

## What This Is

PAPiTA (Palo Alto Inventory Tracking Assistant) — lightweight HTML/JS tool built for a coworker to wrangle Palo Alto firewall auth-code / inventory data. Single-file, browser-only where possible.

## Build & Dev Commands

TBD — spec not yet written. Target stack: vanilla HTML/JS, no build step if avoidable.

## Architecture

TBD — to be defined via `/spec` before any code is written.

## Important Constraints

- Keep it as lean as possible — HTML/JS, minimal dependencies
- Single-user tool for a coworker; no auth, no backend unless unavoidable
- Source PDFs are sensitive work data — never commit sample inputs

## Required Environment Variables

None yet.
