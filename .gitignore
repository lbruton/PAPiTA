# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Env
.env

# Node (we don't use it but guard anyway)
node_modules/

# Git worktrees (per project convention; never commit worktree contents)
.worktrees/

# Playwright MCP session artifacts
.playwright-mcp/

# AI tooling & personal config — not shipped to corp GitLab
# Root-level instruction files (CLAUDE.md / AGENTS.md) ARE tracked —
# they describe the project. Only the tool-specific config dirs stay local.
.claude/*
!.claude/project.json
.gemini/
.codex/
.kimi/
.opencode/
.specflow/
DESIGN.md
specs/

# Sensitive work data — NEVER commit
*.pdf
*.csv
CoWork/

# Allow sample PDFs for testing (mock data only)
!samples/**/*.pdf
